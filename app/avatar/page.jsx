import AvatarClient from './AvatarClient';

export const metadata = {
  title: 'AI Avatar | London Strategy Centre',
  description: 'Talk to the London Strategy Centre AI advisor — powered by HeyGen LiveAvatar.',
};

export default function AvatarPage() {
  return <AvatarClient />;
}
