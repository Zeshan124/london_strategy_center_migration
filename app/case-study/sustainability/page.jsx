import SustainabilityClient from './SustainabilityClient';

export const metadata = {
  title: 'Scope 3 Emissions AI Audit | London Strategy Centre',
  description: "How LSC's Tier 3 agentic system transforms Scope 3 emissions auditing from a manual, point-in-time exercise into a continuous, auditable intelligence capability.",
};

export default function SustainabilityCaseStudyPage() {
  return <SustainabilityClient />;
}
